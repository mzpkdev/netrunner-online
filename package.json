{
  "private": true,
  "name": "netrunner-online",
  "version": "0.0.0",
  "scripts": {
    "serve": "vite serve ./src/",
    "build": "vite build ./src/ --outDir ../dist/ --base \"/netrunner-online/\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "ts-patch": "^3.2.1",
    "vite": "^6.3.4",
    "vitest": "^3.0.0"
  }
}